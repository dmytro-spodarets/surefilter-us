# Database (Postgres + Prisma)
POSTGRES_DB=surefilter
POSTGRES_USER=surefilter
POSTGRES_PASSWORD=devpassword
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}?schema=public"

# Next.js
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# CDN / Assets
# For development: Leave empty or comment out - will use MinIO (http://localhost:9000)
# For production: Set to CloudFront URL
# NEXT_PUBLIC_CDN_URL=https://new.surefilter.us

# MinIO (local S3 for development)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=password123

# AWS (for production only)
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Auth
NEXTAUTH_SECRET=please-change-me
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change-me
