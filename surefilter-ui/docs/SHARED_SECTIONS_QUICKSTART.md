# Shared Sections - Quick Start Guide

## Что это?

**Shared Sections** - переиспользуемые секции контента. Создайте один раз → используйте на многих страницах → изменения применяются везде автоматически.

## Быстрый старт

### 1️⃣ Создать Shared Section

```
Admin → Pages → Shared Sections → New Shared Section
```

1. Введите **Name** (например: "Agriculture Showcase")
2. Выберите **Type** (например: "Industry: Showcase")
3. Добавьте **Description** (опционально)
4. Нажмите **Create**
5. Заполните данные в форме
6. Нажмите **Save**

### 2️⃣ Добавить на страницу

```
Admin → Pages → [Выбрать страницу] → Add Section
```

1. Переключите на **"Use Shared Section"**
2. Выберите нужную shared section
3. Нажмите **"Add Section"**

✅ Готово! Секция появится на странице.

### 3️⃣ Редактировать

**Вариант A:** Из списка
```
Admin → Pages → Shared Sections → Edit
```

**Вариант B:** Со страницы
```
Найдите секцию с фиолетовым badge → Edit Shared
```

⚠️ **Важно:** Изменения применятся на ВСЕХ страницах!

### 4️⃣ Удалить со страницы

```
Секция с фиолетовым badge → Edit → Delete Section
```

Shared section останется в системе, удалится только с этой страницы.

### 5️⃣ Удалить полностью

```
Admin → Pages → Shared Sections → Delete
```

⚠️ Можно удалить только если не используется ни на одной странице.

## Визуальные индикаторы

### 🟣 Фиолетовый badge
Показывает, что секция является shared:
```
1. industry_showcase  [Shared: Agriculture Showcase]  [Edit Shared]
```

### 📊 Счетчик использований
В списке показывает где используется:
```
Agriculture Showcase
Used on 3 page(s): Agriculture, Mining, Construction
```

### ℹ️ Информационная страница
При попытке редактировать shared section со страницы показывается:
- Название и описание
- Предупреждение об изменениях
- Кнопка "Edit Shared Section"
- Кнопка удаления со страницы

## Типичные сценарии

### Сценарий 1: Одинаковый блок на всех индустриях
```
1. Создать shared section "Industry Showcase"
2. Заполнить общие данные (метрики, features)
3. Добавить на все страницы индустрий
4. При изменении метрик - обновится везде
```

### Сценарий 2: Общий Hero для категории
```
1. Создать shared section "Heavy Duty Hero"
2. Настроить изображение и текст
3. Добавить на все страницы Heavy Duty
4. Единая точка управления
```

### Сценарий 3: Переиспользуемый CTA
```
1. Создать shared section "Contact CTA"
2. Настроить призыв к действию
3. Добавить на ключевые страницы
4. A/B тестирование в одном месте
```

## Преимущества

✅ **Консистентность** - одинаковый контент везде  
✅ **Эффективность** - редактируй один раз  
✅ **Безопасность** - нельзя удалить используемую секцию  
✅ **Прозрачность** - видно где используется  
✅ **Гибкость** - работает со всеми типами секций  

## Ограничения

❌ Нельзя удалить shared section, используемую на страницах  
❌ Нельзя иметь разные данные на разных страницах (это shared!)  
❌ Изменения применяются везде (это feature, но будьте осторожны)  

## Когда использовать?

### ✅ Используйте Shared Sections для:
- Повторяющихся блоков на многих страницах
- Контента, который должен быть одинаковым везде
- Блоков, которые часто обновляются
- Стандартизированных секций

### ❌ НЕ используйте для:
- Уникального контента каждой страницы
- Контента, который может отличаться
- Экспериментов на одной странице
- Временных акций на конкретной странице

## Полная документация

Подробная документация: [SHARED_SECTIONS.md](./SHARED_SECTIONS.md)

## Поддержка

При возникновении проблем:
1. Проверьте [Troubleshooting](./SHARED_SECTIONS.md#troubleshooting)
2. Проверьте консоль браузера
3. Сделайте hard reload (Ctrl+Shift+R)
4. Проверьте логи сервера
